import LoginPage from "./LoginPage";

export const metadata = {
    title: "Log in | Agora",
    description: "Access account to post and interact with the community"
}


export default function Login () {
    return (
        <LoginPage/>
    )
}